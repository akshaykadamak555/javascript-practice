/* map() method creates a new array populated with the results of calling a provided function 
on every element in the calling array */

var arr = [2,4,6,8];
var data = arr.map(d => d * 2);
console.log(data);